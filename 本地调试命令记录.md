# 本地调试命令记录

基于编译镜像翻译。

## 获取原码

- 运行容器
  ```bash
  docker run -dit \
  --name flatnotes_zh-cn \
  --entrypoint "tail" \
  dullage/flatnotes:v5.5.4 \
  "-f" "/dev/null"
  ```
- 复制入口文件
  ```bash
  docker cp flatnotes_zh-cn:/entrypoint.sh ./代码
  ```
- 复制前端
  ```bash
  docker cp flatnotes_zh-cn:/app/client/dist ./代码
  ```
- 复制后端文件
  ```bash
  docker cp flatnotes_zh-cn:/app/server/global_config.py ./代码
  ```
- 复制后端搜索文件
  ```bash
  docker cp flatnotes_zh-cn:/app/server/notes/file_system/file_system.py ./代码/file_system.py
  ```
- 删除容器
  ```bash
  docker rm -f flatnotes_zh-cn
  ```

## 运行翻译脚本


## 测试翻译效果

- 运行容器
  ```
  docker run -d \
  --name flatnotes_zh-cn \
  --network bridge \
  --restart no \
  --log-opt max-size=1m \
  --log-opt max-file=1 \
  -p 1234:8080 \
  -e FLATNOTES_SECRET_KEY="aLongRandomSeriesOfCharacters" \
  -e FLATNOTES_AUTH_TYPE=password \
  -e FLATNOTES_USERNAME="firfe" \
  -e FLATNOTES_PASSWORD="firfe" \
  -v ./代码/jieba:/usr/local/lib/python3.11/site-packages/jieba \
  -v ./代码/jieba-0.42.1.dist-info:/usr/local/lib/python3.11/site-packages/jieba-0.42.1.dist-info \
  -v ./代码/entrypoint.sh:/entrypoint.sh \
  -v ./代码/dist:/app/client/dist \
  -v ./代码/global_config.py:/app/server/global_config.py \
  -v ./代码/file_system.py:/app/server/notes/file_system/file_system.py \
  -v ./容器调试数据:/data \
  dullage/flatnotes:v5.5.4
  ```
- 查看实时日志
  ```bash
  docker logs -f flatnotes_zh-cn
  ```
- 删除容器
  ```bash
  docker rm -f flatnotes_zh-cn
  ```

## 编译镜像

- 设置入口文件权限
  ```bash
  chmod +x ./代码/entrypoint.sh
  ```
- 运行编译命令

## 测试容器

- 运行容器
  ```bash
  docker run -d \
  --name flatnotes_zh-cn \
  --network bridge \
  --restart no \
  --log-opt max-size=1m \
  --log-opt max-file=1 \
  -p 1234:8080 \
  -e FLATNOTES_SECRET_KEY="aLongRandomSeriesOfCharacters" \
  -e FLATNOTES_AUTH_TYPE=password \
  -e FLATNOTES_USERNAME="firfe" \
  -e FLATNOTES_PASSWORD="firfe" \
  -v ./容器调试数据:/data \
  firfe/flatnotes_zh-cn:5.5.4
  ```
- 查看实时日志
  ```bash
  docker logs -f flatnotes_zh-cn
  ```
- 删除容器
  ```bash
  docker rm -f flatnotes_zh-cn
  ```
